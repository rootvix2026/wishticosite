(() => {
  const PRODUCTS_FALLBACK = [
    {"id":"premium-cotton-shirt","name":"Premium Cotton Shirt","price":899,"originalPrice":1299,"category":"Shirts","images":["https://images.unsplash.com/photo-1602810316498-ab67cf68c8e1?auto=format&fit=crop&w=900&q=80","https://images.unsplash.com/photo-1603252109303-2751441dd157?auto=format&fit=crop&w=900&q=80"],"sizes":["S","M","L","XL"],"colors":[{"name":"Ivory","hex":"#F5F1EA"},{"name":"Black","hex":"#111111"}],"rating":4.8,"reviews":182,"featured":true,"bestSeller":true,"description":"A refined premium cotton shirt with a soft hand feel, clean structure, and day-to-night versatility."},
    {"id":"casual-check-shirt","name":"Casual Check Shirt","price":699,"originalPrice":999,"category":"Shirts","images":["https://images.unsplash.com/photo-1596755094514-f87e34085b2c?auto=format&fit=crop&w=900&q=80","https://images.unsplash.com/photo-1516826957135-700dedea698c?auto=format&fit=crop&w=900&q=80"],"sizes":["M","L","XL","XXL"],"colors":[{"name":"Navy","hex":"#32506D"},{"name":"Tan","hex":"#8B5E3C"}],"rating":4.6,"reviews":121,"featured":true,"bestSeller":false,"description":"Classic checks tailored with a modern cut for effortless casual polish."},
    {"id":"formal-white-shirt","name":"Formal White Shirt","price":799,"originalPrice":1199,"category":"Shirts","images":["https://images.unsplash.com/photo-1603252109612-24fa03d14562?auto=format&fit=crop&w=900&q=80","https://images.unsplash.com/photo-1602810318383-e386cc2a3ccf?auto=format&fit=crop&w=900&q=80"],"sizes":["S","M","L","XL","XXL"],"colors":[{"name":"White","hex":"#FFFFFF"}],"rating":4.9,"reviews":210,"featured":true,"bestSeller":true,"description":"Sharp, timeless, and impeccably finished for premium formal dressing."},
    {"id":"olive-green-shirt","name":"Olive Green Shirt","price":799,"originalPrice":1099,"category":"Shirts","images":["https://images.unsplash.com/photo-1521572163474-6864f9cf17ab?auto=format&fit=crop&w=900&q=80","https://images.unsplash.com/photo-1507679799987-c73779587ccf?auto=format&fit=crop&w=900&q=80"],"sizes":["S","M","L","XL"],"colors":[{"name":"Olive","hex":"#6F7C59"}],"rating":4.7,"reviews":144,"featured":false,"bestSeller":false,"description":"A rich olive tone paired with clean lines for understated sophistication."},
    {"id":"printed-signature-tee","name":"Printed Signature T-Shirt","price":499,"originalPrice":699,"category":"T-Shirts","images":["https://images.unsplash.com/photo-1521572267360-ee0c2909d518?auto=format&fit=crop&w=900&q=80","https://images.unsplash.com/photo-1576566588028-4147f3842f27?auto=format&fit=crop&w=900&q=80"],"sizes":["S","M","L","XL"],"colors":[{"name":"Black","hex":"#111111"},{"name":"Ivory","hex":"#F5F1EA"}],"rating":4.5,"reviews":96,"featured":true,"bestSeller":true,"description":"Soft premium jersey with bold placement print and elevated everyday comfort."},
    {"id":"essential-oversized-tee","name":"Essential Oversized Tee","price":549,"originalPrice":799,"category":"T-Shirts","images":["https://images.unsplash.com/photo-1503342217505-b0a15ec3261c?auto=format&fit=crop&w=900&q=80","https://images.unsplash.com/photo-1529139574466-a303027c1d8b?auto=format&fit=crop&w=900&q=80"],"sizes":["M","L","XL","XXL"],"colors":[{"name":"Stone","hex":"#DDD1C1"},{"name":"Charcoal","hex":"#2E2E2E"}],"rating":4.6,"reviews":88,"featured":false,"bestSeller":false,"description":"Relaxed proportions, premium weight, and an ultra-clean silhouette."},
    {"id":"black-tailored-pant","name":"Black Tailored Pant","price":899,"originalPrice":1299,"category":"Pants","images":["https://images.unsplash.com/photo-1506629905607-24339d0bb38b?auto=format&fit=crop&w=900&q=80","https://images.unsplash.com/photo-1473966968600-fa801b869a1a?auto=format&fit=crop&w=900&q=80"],"sizes":["M","L","XL","XXL"],"colors":[{"name":"Black","hex":"#111111"}],"rating":4.8,"reviews":172,"featured":true,"bestSeller":true,"description":"Tailored for a sleek line with stretch comfort and premium drape."},
    {"id":"sand-relaxed-pant","name":"Sand Relaxed Pant","price":849,"originalPrice":1199,"category":"Pants","images":["https://images.unsplash.com/photo-1514996937319-344454492b37?auto=format&fit=crop&w=900&q=80","https://images.unsplash.com/photo-1503341455253-b2e723bb3dbb?auto=format&fit=crop&w=900&q=80"],"sizes":["S","M","L","XL"],"colors":[{"name":"Sand","hex":"#C7B39A"}],"rating":4.4,"reviews":73,"featured":false,"bestSeller":false,"description":"Relaxed tailoring in a versatile neutral tone for elevated daily styling."},
    {"id":"denim-jacket","name":"Denim Jacket","price":1299,"originalPrice":1799,"category":"Jackets","images":["https://images.unsplash.com/photo-1523398002811-999ca8dec234?auto=format&fit=crop&w=900&q=80","https://images.unsplash.com/photo-1541099649105-f69ad21f3246?auto=format&fit=crop&w=900&q=80"],"sizes":["M","L","XL"],"colors":[{"name":"Indigo","hex":"#33547A"}],"rating":4.9,"reviews":204,"featured":true,"bestSeller":true,"description":"An elevated denim layer with structured shoulders and a polished wash."},
    {"id":"luxe-bomber-jacket","name":"Luxe Bomber Jacket","price":1499,"originalPrice":2199,"category":"Jackets","images":["https://images.unsplash.com/photo-1483985988355-763728e1935b?auto=format&fit=crop&w=900&q=80","https://images.unsplash.com/photo-1512436991641-6745cdb1723f?auto=format&fit=crop&w=900&q=80"],"sizes":["M","L","XL","XXL"],"colors":[{"name":"Black","hex":"#111111"},{"name":"Olive","hex":"#6F7C59"}],"rating":4.7,"reviews":138,"featured":false,"bestSeller":false,"description":"Minimal luxe bomber with clean hardware and a sharply contemporary fit."},
    {"id":"combo-pack-3","name":"Combo Pack 3","price":1999,"originalPrice":2899,"category":"Combo Offers","images":["https://images.unsplash.com/photo-1496747611176-843222e1e57c?auto=format&fit=crop&w=900&q=80","https://images.unsplash.com/photo-1483985988355-763728e1935b?auto=format&fit=crop&w=900&q=80"],"sizes":["S","M","L","XL"],"colors":[{"name":"Mixed","hex":"#C9A36A"}],"rating":4.8,"reviews":166,"featured":true,"bestSeller":true,"description":"Three premium essentials bundled into one polished value-driven set."},
    {"id":"weekend-duo-offer","name":"Weekend Duo Offer","price":1399,"originalPrice":1999,"category":"Combo Offers","images":["https://images.unsplash.com/photo-1488161628813-04466f872be2?auto=format&fit=crop&w=900&q=80","https://images.unsplash.com/photo-1529139574466-a303027c1d8b?auto=format&fit=crop&w=900&q=80"],"sizes":["M","L","XL"],"colors":[{"name":"Mixed","hex":"#B88C58"}],"rating":4.6,"reviews":84,"featured":false,"bestSeller":false,"description":"A premium two-piece edit assembled for easy wardrobe upgrades."}
  ];

  const state = { products: [], homeCategory: 'All', collectionPage: 1, initialCategoryApplied: false, filters: { category: 'All', size: '', color: '', price: 2500, sort: 'popular' } };
  const currency = new Intl.NumberFormat('en-IN', { style: 'currency', currency: 'INR', maximumFractionDigits: 0 });
  const query = new URLSearchParams(window.location.search);
  const formatPrice = (value) => currency.format(value);
  const discount = (product) => Math.round(((product.originalPrice - product.price) / product.originalPrice) * 100);
  const loadProducts = async () => { try { const response = await fetch('assets/data/products.json'); if (!response.ok) throw new Error(`Failed to load products: ${response.status}`); return response.json(); } catch { return PRODUCTS_FALLBACK; } };
  const cardTemplate = (product) => `<article class="product-card reveal"><div class="product-card__media"><span class="product-card__badge">-${discount(product)}%</span><button class="product-card__wishlist" type="button" data-wishlist="${product.id}" aria-label="Toggle wishlist">♡</button><img src="${product.images[0]}" alt="${product.name}" /><img src="${product.images[1] || product.images[0]}" alt="${product.name} alternate view" /><a class="quick-view-link" href="product.html?id=${product.id}" aria-label="Quick view ${product.name}">→</a></div><div class="product-card__body"><div class="product-card__meta"><div><p>${product.category}</p><h3 class="product-card__title">${product.name}</h3></div><span class="product-card__rating">★ ${product.rating}</span></div><div class="price-row"><strong>${formatPrice(product.price)}</strong><s>${formatPrice(product.originalPrice)}</s></div><div class="product-card__footer"><a class="button button-secondary" href="product.html?id=${product.id}">View</a><button class="button button-primary" type="button" data-add-to-cart="${product.id}">Add to Cart</button></div></div></article>`;
  const refreshReveal = () => document.querySelectorAll('.reveal').forEach((item) => item.classList.add('is-visible'));
  const attachProductActions = () => {
    document.querySelectorAll('[data-add-to-cart]').forEach((button) => button.addEventListener('click', () => {
      const product = state.products.find((item) => item.id === button.dataset.addToCart); if (!product) return;
      window.WishTicoCart?.addItem({ id: product.id, name: product.name, price: product.price, image: product.images[0], size: product.sizes[0], color: product.colors[0].hex, colorName: product.colors[0].name, quantity: 1 });
    }));
    document.querySelectorAll('[data-wishlist]').forEach((button) => button.addEventListener('click', () => { button.classList.toggle('is-active'); button.textContent = button.classList.contains('is-active') ? '♥' : '♡'; }));
  };
  const renderHome = () => {
    const tabsRoot = document.getElementById('home-category-tabs');
    const homeGrid = document.getElementById('home-product-grid');
    const featuredGrid = document.getElementById('featured-product-grid');
    if (tabsRoot) { const categories = ['All', 'Shirts', 'T-Shirts', 'Pants', 'Jackets', 'Combo Offers']; tabsRoot.innerHTML = categories.map((category) => `<button class="filter-tab ${category === state.homeCategory ? 'is-active' : ''}" type="button" data-home-category="${category}">${category}</button>`).join(''); tabsRoot.querySelectorAll('[data-home-category]').forEach((button) => button.addEventListener('click', () => { state.homeCategory = button.dataset.homeCategory; renderHome(); })); }
    if (homeGrid) { const filtered = (state.homeCategory === 'All' ? state.products : state.products.filter((product) => product.category === state.homeCategory)).slice(0, 8); homeGrid.innerHTML = filtered.map(cardTemplate).join(''); }
    if (featuredGrid) featuredGrid.innerHTML = state.products.filter((product) => product.bestSeller).slice(0, 4).map(cardTemplate).join('');
    attachProductActions(); refreshReveal();
  };
  const renderCollections = () => {
    const grid = document.getElementById('collection-grid'); if (!grid) return;
    const startCategory = query.get('category'); if (startCategory && !state.initialCategoryApplied) { state.filters.category = startCategory; state.initialCategoryApplied = true; }
    const categoryRoot = document.getElementById('filter-category-list');
    const categories = ['All', ...new Set(state.products.map((product) => product.category))];
    if (categoryRoot) { categoryRoot.innerHTML = categories.map((category) => { const count = category === 'All' ? state.products.length : state.products.filter((product) => product.category === category).length; return `<button class="filter-tab ${state.filters.category === category ? 'is-active' : ''}" type="button" data-collection-category="${category}">${category} <span>(${count})</span></button>`; }).join(''); categoryRoot.querySelectorAll('[data-collection-category]').forEach((button) => button.addEventListener('click', () => { state.filters.category = button.dataset.collectionCategory; renderCollections(); })); }
    const priceRange = document.getElementById('price-range'); const priceValue = document.getElementById('price-range-value'); const sortSelect = document.getElementById('collection-sort'); if (priceRange) priceRange.value = String(state.filters.price); if (priceValue) priceValue.textContent = formatPrice(state.filters.price); if (sortSelect) sortSelect.value = state.filters.sort;
    let filtered = state.products.filter((product) => product.price <= state.filters.price);
    if (state.filters.category !== 'All') filtered = filtered.filter((product) => product.category === state.filters.category);
    if (state.filters.size) filtered = filtered.filter((product) => product.sizes.includes(state.filters.size));
    if (state.filters.color) filtered = filtered.filter((product) => product.colors.some((color) => color.hex === state.filters.color));
    switch (state.filters.sort) { case 'newest': filtered = [...filtered].reverse(); break; case 'price-asc': filtered = [...filtered].sort((a, b) => a.price - b.price); break; case 'price-desc': filtered = [...filtered].sort((a, b) => b.price - a.price); break; case 'discount': filtered = [...filtered].sort((a, b) => discount(b) - discount(a)); break; default: filtered = [...filtered].sort((a, b) => Number(b.bestSeller) - Number(a.bestSeller)); }
    const perPage = 12; const totalPages = Math.max(1, Math.ceil(filtered.length / perPage)); state.collectionPage = Math.min(state.collectionPage, totalPages); const paginated = filtered.slice((state.collectionPage - 1) * perPage, state.collectionPage * perPage);
    grid.innerHTML = paginated.length ? paginated.map(cardTemplate).join('') : '<div class="empty-state"><h2>No matching products</h2><p>Try clearing your filters for a wider premium edit.</p></div>';
    document.getElementById('collection-count').textContent = String(filtered.length);
    const pagination = document.getElementById('collection-pagination'); pagination.innerHTML = Array.from({ length: totalPages }, (_, index) => `<button type="button" class="${state.collectionPage === index + 1 ? 'is-active' : ''}" data-page="${index + 1}">${index + 1}</button>`).join(''); pagination.querySelectorAll('[data-page]').forEach((button) => button.addEventListener('click', () => { state.collectionPage = Number(button.dataset.page); renderCollections(); window.scrollTo({ top: 0, behavior: 'smooth' }); }));
    attachProductActions(); refreshReveal();
  };
  const initCollectionControls = () => {
    const sizeButtons = document.querySelectorAll('[data-size]'); sizeButtons.forEach((button) => button.addEventListener('click', () => { sizeButtons.forEach((item) => item.classList.remove('is-active')); button.classList.add('is-active'); state.filters.size = button.dataset.size; }));
    const colorButtons = document.querySelectorAll('#color-filter-group .swatch'); colorButtons.forEach((button) => button.addEventListener('click', () => { colorButtons.forEach((item) => item.classList.remove('is-active')); button.classList.add('is-active'); state.filters.color = button.dataset.color; }));
    document.getElementById('price-range')?.addEventListener('input', (event) => { state.filters.price = Number(event.target.value); document.getElementById('price-range-value').textContent = formatPrice(state.filters.price); });
    document.getElementById('collection-sort')?.addEventListener('change', (event) => { state.filters.sort = event.target.value; renderCollections(); });
    document.getElementById('apply-filters')?.addEventListener('click', () => { state.collectionPage = 1; renderCollections(); });
    document.getElementById('clear-filters')?.addEventListener('click', () => { state.filters = { category: 'All', size: '', color: '', price: 2500, sort: 'popular' }; document.querySelectorAll('#size-filter-group .chip, #color-filter-group .swatch').forEach((button) => button.classList.remove('is-active')); state.collectionPage = 1; renderCollections(); });
  };
  const renderProductDetail = () => {
    const root = document.getElementById('product-detail-root'); if (!root) return;
    const product = state.products.find((item) => item.id === query.get('id')) || state.products[0]; if (!product) return;
    const breadcrumb = document.getElementById('product-breadcrumb'); if (breadcrumb) breadcrumb.textContent = `Home / Collections / ${product.category} / ${product.name}`;
    root.innerHTML = `<div class="product-detail reveal"><div class="product-gallery"><div class="product-gallery__main"><img id="product-main-image" src="${product.images[0]}" alt="${product.name}" /></div><div class="product-gallery__thumbs">${product.images.map((image, index) => `<button type="button" data-thumb="${image}" aria-label="View ${product.name} image ${index + 1}"><img src="${image}" alt="${product.name} thumbnail ${index + 1}" /></button>`).join('')}</div></div><div class="product-summary"><span class="eyebrow">${product.category}</span><h1>${product.name}</h1><div class="rating-row"><span>★ ${product.rating}</span><span>(${product.reviews} reviews)</span></div><div class="price-row"><strong>${formatPrice(product.price)}</strong><s>${formatPrice(product.originalPrice)}</s><span class="eyebrow">Save ${discount(product)}%</span></div><p>${product.description}</p><div><h3>Colours</h3><div class="color-selector" id="product-color-selector">${product.colors.map((color, index) => `<button type="button" class="swatch ${index === 0 ? 'is-active' : ''}" data-product-color="${color.hex}" data-color-name="${color.name}" style="--swatch:${color.hex}" aria-label="${color.name}"></button>`).join('')}</div></div><div><h3>Size</h3><div class="size-selector" id="product-size-selector">${product.sizes.map((size, index) => `<button type="button" class="chip ${index === 0 ? 'is-active' : ''}" data-product-size="${size}">${size}</button>`).join('')}</div></div><div><h3>Quantity</h3><div class="quantity-stepper"><button type="button" data-product-qty="-1">−</button><span id="product-qty">1</span><button type="button" data-product-qty="1">+</button></div></div><div class="product-actions"><button class="button button-primary" type="button" id="product-add-to-cart">Add to Cart</button><button class="button button-secondary" type="button" id="product-buy-now">Buy Now</button></div><div class="trust-badges"><span>Free Shipping</span><span>Easy Returns</span><span>Secure Payment</span></div></div></div>`;
    const selected = { size: product.sizes[0], color: product.colors[0].hex, colorName: product.colors[0].name, quantity: 1 };
    root.querySelectorAll('[data-thumb]').forEach((button) => button.addEventListener('click', () => { document.getElementById('product-main-image').src = button.dataset.thumb; }));
    root.querySelectorAll('[data-product-size]').forEach((button) => button.addEventListener('click', () => { root.querySelectorAll('[data-product-size]').forEach((item) => item.classList.remove('is-active')); button.classList.add('is-active'); selected.size = button.dataset.productSize; }));
    root.querySelectorAll('[data-product-color]').forEach((button) => button.addEventListener('click', () => { root.querySelectorAll('[data-product-color]').forEach((item) => item.classList.remove('is-active')); button.classList.add('is-active'); selected.color = button.dataset.productColor; selected.colorName = button.dataset.colorName; }));
    root.querySelectorAll('[data-product-qty]').forEach((button) => button.addEventListener('click', () => { selected.quantity = Math.max(1, selected.quantity + Number(button.dataset.productQty)); document.getElementById('product-qty').textContent = String(selected.quantity); }));
    const add = () => window.WishTicoCart?.addItem({ id: product.id, name: product.name, price: product.price, image: product.images[0], quantity: selected.quantity, size: selected.size, color: selected.color, colorName: selected.colorName });
    document.getElementById('product-add-to-cart')?.addEventListener('click', add);
    document.getElementById('product-buy-now')?.addEventListener('click', () => { add(); document.dispatchEvent(new Event('cart:open')); });
    const related = document.getElementById('related-product-grid'); if (related) related.innerHTML = state.products.filter((item) => item.category === product.category && item.id !== product.id).slice(0, 4).map(cardTemplate).join('');
    attachProductActions(); refreshReveal();
  };
  document.addEventListener('DOMContentLoaded', async () => { state.products = await loadProducts(); renderHome(); initCollectionControls(); renderCollections(); renderProductDetail(); });
})();
